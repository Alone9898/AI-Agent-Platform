{
  "name": "ai-agent-platform",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "tauri": "tauri",
    "nestjs:build": "cd nestjs && npm run build",
    "nestjs:dev": "cd nestjs && npm run start:dev"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  }
}
